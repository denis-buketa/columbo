apply plugin: "com.android.application"
apply from: "$rootDir/buildscripts/commonAndroid.gradle"

android {
    defaultConfig {
        applicationId "denisbuketa.android.columbo"
    }
}

dependencies {
    implementation appcompat
}